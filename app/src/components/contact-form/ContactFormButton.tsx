import React from 'react';

interface ContactFormButtonProps {
    title: string;
    isLoading: boolean;
    isSuccess: boolean;
}

const ContactFormButton: React.FC<ContactFormButtonProps> = (props) => {
    return <>This is a ReactFC!</>
}

export default ContactFormButton;